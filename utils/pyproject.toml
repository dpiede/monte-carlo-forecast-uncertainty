[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "utils"
version = "0.1.0"
description = "Utility functions"

[tool.hatch.build.targets.wheel]
packages = ["."]
